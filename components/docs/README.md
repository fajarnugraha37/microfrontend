# Documentation Index

This `docs` folder contains technical documentation and migration guidance for the project. The most relevant document for state bridging during migration is:

- **Stores: Vuex ↔ Pinia Bridge Architecture & Migration Guide** (`stores-wiki.md`)

Planned docs additions:
- Example migration guide & code snippets for a `user` module.
- Example microfrontend (Vue 3) + host (Vue 2) app demo project demonstrating end-to-end bridging.

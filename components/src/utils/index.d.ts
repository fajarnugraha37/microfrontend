// Merged into global.d.ts - placeholder to avoid retyping
export function getModuleState(rootState: Record<string, any>, namespace: string): any;

export function deepPatch(target: any, patch: any): void;
